#include "SpaceShip.h"

SpaceShip::SpaceShip(SDL_Renderer* rend):GameObject(rend, new Vector2Int(0, 0), new Vector2Int(30, 40)) {



}

void SpaceShip::Update(float dt) {

	//position = position + (linealVelocity * dt);

}